# Required for the writing exercise loop (prompt generation + grading).
# Get one at https://console.anthropic.com
ANTHROPIC_API_KEY=

# SQLite by default; swap for a Postgres URL when deploying.
DATABASE_URL="file:./dev.db"
